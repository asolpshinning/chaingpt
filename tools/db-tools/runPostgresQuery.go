package dbtools

func RunPostgresQuery(query string) (string, error) {
	// put the code the sends the text to GPT-3 and returns the SQL code or a message that says no

	return `{Friday : 32, Sunday : 48}`, nil
}
